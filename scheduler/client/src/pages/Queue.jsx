// Deliverable 2: per-platform queue with priority and ordering.
// TODO(Sprint 4): list per platform, reorder, priority toggle, override date.
export default function Queue() {
  return (
    <section>
      <h1>Queue</h1>
      <p className="todo">Platform queues go here.</p>
    </section>
  );
}
